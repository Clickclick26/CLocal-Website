# Waitlist ingest robot

Saves CLocal waitlist signups and emails from `hello@clocal.co.uk` via Titan SMTP.

Deploy:

```bash
supabase functions deploy waitlist-ingest --project-ref gapybapywpdogexibtgj
```

See root `README.md` for secrets and SQL.
